{
  "name": "labeler",
  "scripts": {
    "build": "esbuild src/action.ts --bundle --format=cjs --platform=node --legal-comments=none --outfile=src/action.js"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^5.1.1",
    "@types/node": "^20.6.0",
    "esbuild": "^0.19.3",
    "typescript": "^5.2.2",
    "yaml": "^2.3.2"
  }
}
